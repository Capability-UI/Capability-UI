# Meridian

A sales CRM: an open-deal pipeline board, a company account book, notes, and owner-only email. Bob never sees phone, personal email, or deal value.

http://127.0.0.1:8782 · Data explorer at `/explorer.html`

```bash
cd examples && npm install && npm run crm
```
