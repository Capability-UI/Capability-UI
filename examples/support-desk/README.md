# Clearline

An IT service desk: ticket queue, conversation thread, assign, and escalate. Contractors do not see internal notes and cannot escalate.

http://127.0.0.1:8783 · Data explorer at `/explorer.html`

```bash
cd examples && npm install && npm run support
```
